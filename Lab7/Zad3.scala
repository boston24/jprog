package Lab7

object Zad3 extends App {
/*
  val seq = Seq(1, 1, 2, 4, 4, 4, 1, 3)

  def deStutter[A](seq: Seq[A]): Seq[A] = {
    seq.foldLeft[Seq[A]](Seq())((acc,a) => if(acc.filter(x => x=a)==Seq()) acc :+ a )
  }

  println(deStutter(seq))
*/
}
